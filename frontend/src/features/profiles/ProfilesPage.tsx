import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {http} from '../../api/http';import {useNavigate} from 'react-router-dom';import {useState} from 'react';
export const ProfilesPage=()=>{const nav=useNavigate();const qc=useQueryClient();const [name,setName]=useState('');const q=useQuery({queryKey:['profiles'],queryFn:async()=> (await http.get('/api/profiles')).data}); const create=useMutation({mutationFn:()=>http.post('/api/profiles',{name,scope:'PERSONAL'}),onSuccess:()=>qc.invalidateQueries({queryKey:['profiles']})}); return <div><h1>Profiles</h1><input value={name} onChange={e=>setName(e.target.value)}/><button onClick={()=>create.mutate()}>Crear</button><ul>{q.data?.map((p:any)=><li key={p.id}><button onClick={()=>{localStorage.setItem('selectedProfileId',p.id);nav(`/profiles/${p.id}/dashboard`)}}>Entrar</button>{p.name}</li>)}</ul></div>}
