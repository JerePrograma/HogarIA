import {useQuery,useMutation,useQueryClient} from '@tanstack/react-query';import {http} from '../../api/http';import {useNavigate} from 'react-router-dom';import {useState} from 'react';
export const DevUserPage=()=>{const nav=useNavigate();const qc=useQueryClient();const [name,setName]=useState('');const [email,setEmail]=useState('');const q=useQuery({queryKey:['dev-users'],queryFn:async()=> (await http.get('/api/dev-users')).data}); const m=useMutation({mutationFn:()=>http.post('/api/dev-users',{name,email}),onSuccess:()=>qc.invalidateQueries({queryKey:['dev-users']})});return <div><h1>Dev users</h1><input placeholder='name' value={name} onChange={e=>setName(e.target.value)}/><input placeholder='email' value={email} onChange={e=>setEmail(e.target.value)}/><button onClick={()=>m.mutate()}>Crear</button><ul>{q.data?.map((u:any)=><li key={u.id}><button onClick={()=>{localStorage.setItem('devUserId',u.id);nav('/profiles')}}>Seleccionar</button> {u.name}</li>)}</ul></div>}
