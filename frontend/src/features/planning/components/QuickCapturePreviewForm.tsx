import { monthlyPlanPriorityOptions, monthlyPlanStatusOptions, monthlyPlanTypeOptions } from '../../../domain/financeOptions';
import type { Account, Category, MonthlyPlanItem, MonthlyPlanItemCreatePayload, QuickCapturePreviewResponse } from '../../../domain/types';
import { StatusBadge } from '../../../components/ui/StatusBadge';
import { confidenceMeta, formatPlanAmount, formatPlanRecovery } from '../planningUtils';

type Props = { preview: QuickCapturePreviewResponse; form: MonthlyPlanItemCreatePayload; setForm: (next: MonthlyPlanItemCreatePayload) => void; accounts: Account[]; categories: Category[]; onConfirm: () => void };
export function QuickCapturePreviewForm({ preview, form, setForm, accounts, categories, onConfirm }: Props) { const confidence = confidenceMeta(preview.confidence); return <div className='page-stack'><p><b>Confianza:</b> <span className={`badge ${confidence.className}`}>{confidence.label}</span></p>{preview.warnings.length > 0 && <ul>{preview.warnings.map((w, i) => <li key={i}>{w}</li>)}</ul>}<div className='form-grid'><select className='select' value={form.type} onChange={e => setForm({ ...form, type: e.target.value as MonthlyPlanItem['type'] })}>{monthlyPlanTypeOptions.map(o => <option key={o.value} value={o.value}>{o.label}</option>)}</select><input className='input' value={form.title} onChange={e => setForm({ ...form, title: e.target.value })} placeholder='Título' /><select className='select' value={form.priority} onChange={e => setForm({ ...form, priority: e.target.value as MonthlyPlanItem['priority'] })}>{monthlyPlanPriorityOptions.map(o => <option key={o.value} value={o.value}>{o.label}</option>)}</select><select className='select' value={form.status} onChange={e => setForm({ ...form, status: e.target.value as MonthlyPlanItem['status'] })}>{monthlyPlanStatusOptions.map(o => <option key={o.value} value={o.value}>{o.label}</option>)}</select><select className='select' value={form.accountId ?? ''} onChange={e => setForm({ ...form, accountId: e.target.value || null })}><option value=''>Cuenta opcional</option>{accounts.map((a) => <option key={a.id} value={a.id}>{a.name}</option>)}</select><select className='select' value={form.categoryId ?? ''} onChange={e => setForm({ ...form, categoryId: e.target.value || null })}><option value=''>Categoría opcional</option>{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div><p>Monto detectado: {formatPlanAmount(form as MonthlyPlanItem)}</p><p>Recupero detectado: {formatPlanRecovery(form as MonthlyPlanItem)}</p><button className='button-primary' onClick={onConfirm} disabled={!form.title.trim()}>Confirmar y crear</button></div>; }
