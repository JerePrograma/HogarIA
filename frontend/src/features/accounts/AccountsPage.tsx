import {useParams} from 'react-router-dom';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {http} from '../../api/http';import {useState} from 'react';
export const AccountsPage=()=>{const {profileId}=useParams();const qc=useQueryClient();const [name,setName]=useState('');const q=useQuery({queryKey:['accounts',profileId],queryFn:async()=> (await http.get(`/api/profiles/${profileId}/accounts`)).data});const m=useMutation({mutationFn:()=>http.post(`/api/profiles/${profileId}/accounts`,{name,accountType:'CASH',currency:'ARS'}),onSuccess:()=>qc.invalidateQueries({queryKey:['accounts',profileId]})});return <div><h1>Accounts</h1><input value={name} onChange={e=>setName(e.target.value)}/><button onClick={()=>m.mutate()}>Crear</button><ul>{q.data?.map((a:any)=><li key={a.id}>{a.name}</li>)}</ul></div>}
