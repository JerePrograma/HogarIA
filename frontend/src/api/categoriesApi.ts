import {http} from './http'; export const listCategories=(pid:string,includeGlobal:boolean)=>http.get(`/api/profiles/${pid}/categories?includeGlobal=${includeGlobal}`).then(r=>r.data); export const createCategory=(pid:string,p:unknown)=>http.post(`/api/profiles/${pid}/categories`,p).then(r=>r.data); export const getCategory=(id:string)=>http.get(`/api/categories/${id}`).then(r=>r.data); export const updateCategory=(id:string,p:unknown)=>http.put(`/api/categories/${id}`,p).then(r=>r.data); export const deleteCategory=(id:string)=>http.delete(`/api/categories/${id}`).then(r=>r.data);
