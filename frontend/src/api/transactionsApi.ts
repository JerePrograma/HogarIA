import {http} from './http'; export const listTransactions=(pid:string,year:number,month:number)=>http.get(`/api/profiles/${pid}/transactions?year=${year}&month=${month}`).then(r=>r.data); export const createTransaction=(p:unknown)=>http.post('/api/transactions',p).then(r=>r.data); export const getTransaction=(id:string)=>http.get(`/api/transactions/${id}`).then(r=>r.data); export const updateTransaction=(id:string,p:unknown)=>http.put(`/api/transactions/${id}`,p).then(r=>r.data); export const deleteTransaction=(id:string)=>http.delete(`/api/transactions/${id}`).then(r=>r.data);
