import {http} from './http'; export const listProfiles=()=>http.get('/api/profiles').then(r=>r.data); export const createProfile=(p:unknown)=>http.post('/api/profiles',p).then(r=>r.data); export const getProfile=(id:string)=>http.get(`/api/profiles/${id}`).then(r=>r.data); export const updateProfile=(id:string,p:unknown)=>http.put(`/api/profiles/${id}`,p).then(r=>r.data); export const deleteProfile=(id:string)=>http.delete(`/api/profiles/${id}`).then(r=>r.data);
