import {http} from './http'; export const getMonthlyPlan=(profileId:string,year:number,month:number)=>http.get(`/api/profiles/${profileId}/planning/monthly?year=${year}&month=${month}`).then(r=>r.data); export const createMonthlyPlanItem=(profileId:string,payload:unknown)=>http.post(`/api/profiles/${profileId}/planning/items`,payload).then(r=>r.data); export const updateMonthlyPlanItem=(profileId:string,itemId:string,payload:unknown)=>http.put(`/api/profiles/${profileId}/planning/items/${itemId}`,payload).then(r=>r.data); export const deleteMonthlyPlanItem=(profileId:string,itemId:string)=>http.delete(`/api/profiles/${profileId}/planning/items/${itemId}`).then(r=>r.data); export const convertMonthlyPlanItemToTransaction=(profileId:string,itemId:string)=>http.post(`/api/profiles/${profileId}/planning/items/${itemId}/convert-to-transaction`).then(r=>r.data);
