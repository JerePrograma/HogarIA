package com.hogaria.repository; import com.hogaria.entity.BudgetYear; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface BudgetYearRepository extends JpaRepository<BudgetYear,UUID>{ Optional<BudgetYear> findByProfileIdAndYear(UUID profileId,Integer year); List<BudgetYear> findByProfileId(UUID profileId); boolean existsByProfileIdAndYear(UUID profileId,Integer year);} 
