package com.hogaria.entity;import jakarta.persistence.*;import lombok.*;import org.hibernate.annotations.CreationTimestamp;import org.hibernate.annotations.UpdateTimestamp;import java.math.*;import java.time.*;import java.util.*;
@Entity @Table(name="inflation_index") @Data @Builder @NoArgsConstructor @AllArgsConstructor public class InflationIndex { @Id @GeneratedValue private UUID id; private Integer year; private Integer month; private BigDecimal rate; @Enumerated(EnumType.STRING) private InflationSource source; private Boolean observed; private String notes; @CreationTimestamp private LocalDateTime createdAt; @UpdateTimestamp private LocalDateTime updatedAt; }
